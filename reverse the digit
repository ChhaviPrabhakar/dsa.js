function reverse(n){
    /* Function to return the reverse of a number n */
    
    var rev=0;
    var d;
    
    while(n>0)
    {
        d=n%10;
        rev=rev*10+d;
        n=Math.floor(n/10);
    }
    
    return rev;
}
